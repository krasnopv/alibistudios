# Nginx: staging.alibistudios.co → Docker port 3300
# Use after: 1) DNS A record for staging.alibistudios.co, 2) certbot for staging

# HTTP → HTTPS (uncomment after SSL is in place)
# server {
#     listen 80;
#     listen [::]:80;
#     server_name staging.alibistudios.co;
#     return 301 https://$server_name$request_uri;
# }

# Temporary HTTP (for certbot; remove or replace with block above after SSL)
server {
    listen 80;
    listen [::]:80;
    server_name staging.alibistudios.co;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://localhost:3300;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# HTTPS: use same cert as production (expand first: certbot --nginx -d alibistudios.co -d www.alibistudios.co -d staging.alibistudios.co --expand)
# Or separate cert: certbot --nginx -d staging.alibistudios.co → then use /etc/letsencrypt/live/staging.alibistudios.co/
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name staging.alibistudios.co;
#
#     ssl_certificate /etc/letsencrypt/live/alibistudios.co/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/alibistudios.co/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     access_log /var/log/nginx/staging-alibi-access.log;
#     error_log /var/log/nginx/staging-alibi-error.log;
#
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_types text/plain text/css text/xml text/javascript application/javascript application/json;
#     client_max_body_size 20M;
#
#     location / {
#         proxy_pass http://localhost:3300;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         proxy_pass http://localhost:3300;
#         proxy_cache_valid 200 30d;
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     location /health {
#         access_log off;
#         proxy_pass http://localhost:3300/health;
#     }
# }
